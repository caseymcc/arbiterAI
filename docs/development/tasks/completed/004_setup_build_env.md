# Task: Setup Build Environment

## Description
The current `build.sh` script assumes a specific environment (likely Docker) with `vcpkg` at `/opt/vcpkg`. This is not present in the current dev environment.

## Goals
- [ ] Update `build.sh` or provide instructions for local build.
- [ ] Configure `vcpkg` for local development.
- [ ] Ensure `cmake` can find dependencies (`cpr`, `nlohmann_json`, etc.).

## Status
Todo
